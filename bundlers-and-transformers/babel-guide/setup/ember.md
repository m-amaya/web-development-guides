# Setup Babel with Ember

## Installation

```bash
$ npm install --save-dev ember-cli-babel
```

## Create `.babelrc` Configuration File

Save a preset:
```bash
$ npm install babel-preset-env --save-dev
```

Enable presets in `.babelrc` file:
```json
{
    "presets": [ "env" ]
}
```
